# Write a program that takes a list of words and joins them into a single string, 
# separated by a character (e.g., `-` or `,`).  

a=str(input("Enter a word: "))
b=str(input("Enter some words: "))

print(f"{a}-{b}")